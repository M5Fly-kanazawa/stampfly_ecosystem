# SILS host build artifacts / SILS ホストビルドの生成物
build/
build-mingw/
_deps/
*.o
*.a
*.out
# Fetched MuJoCo source (acquired via FetchContent, not vendored)
# FetchContent で取得する MuJoCo ソース（コピー同梱しない）
mujoco-build/
simulator/sils/viz/venv/
